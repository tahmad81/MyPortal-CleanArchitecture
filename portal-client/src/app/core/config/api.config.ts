/**
 * Central place to configure the base URL for the Portal API backend.
 * Update this value to reflect the host/port where the ASP.NET API is running.
 */
export const apiConfig = {
  baseUrl: 'https://localhost:7031/api'
};

